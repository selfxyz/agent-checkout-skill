{
  "name": "@selfxyz/agent-checkout-skill",
  "version": "0.1.0",
  "description": "Open registry of machine-readable checkout recipes for browser agents.",
  "license": "MIT",
  "type": "module",
  "files": ["recipes", "schema", "src", "dist"],
  "scripts": {
    "validate": "bun run scripts/validate.ts",
    "build": "bun run scripts/build.ts",
    "test": "bun test",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
